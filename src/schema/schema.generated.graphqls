type Book {
  author: String
  id: ID!
  isbn: String!
  title: String
}

type Mutation {
  markBookAsRead1(author: String!, id: ID!, isbn: String!, title: String!): Book
}

type Query {
  book(id1: ID!): Book
  user(id: ID!): User
}

type Qurey

type User {
  email: String!
  firstName: String!
  id: ID!
  lastName: String!
}