type Query {
    book(id: ID!): Book
}

extend type Mutation {
    markBookAsRead(id: ID!, title: String!, author: String!, isbn: String!): Book
}

type Book {
    id: ID!
    title: String
    author: String
    isbn: String!
    # other fields...
}
