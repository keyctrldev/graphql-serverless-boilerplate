extend type Query {
  user(id: ID!): User
}

type User {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
}

extend type Mutation {
  createUser(id: ID!, firstName: String!, lastName: String!, email: String!): User
}
